# App Idea

an app that can be used by an AI agent to work with ansible.

the project should be designed to guide an AI into using a bash tool to run ansible.  The AI should be able to load the repo, and start working with an ansible inventory, roles, playbooks in under 2 minutes.
